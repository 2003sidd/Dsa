#include<stdio.h>

main(){
    // Data type : It is defined the type of value a variable can store and the size of memory required
    // to store that variable in memory 
    // int family this family contan int , float, double and char 
    // there are some modifers to change their range (signed and unsigned) and its capaity or size (long , short 
    // and long long) 
    int a=5;
    short int b=5;
    long int c=5;
    long long int d=5;
    // printf("size of a=%d\n",sizeof(a));
    // printf("size of b=%d\n",sizeof(b));
    // printf("size of c=%d\n",sizeof(c));
    // printf("size of d=%d\n",sizeof(d));
    float f=2.2;
    double g=2.2;
    long double h=2.22;
    // printf("size of f=%d\n",sizeof(f));
    // printf("size of g=%d\n",sizeof(g ));
    // printf("size of h=%d\n",sizeof(h ));
    // printf("%f %lf %Lf",f,g,h);
    char ch='c';
    char ch1='f';
    // printf("a=%d\n")
    // float b=5;
    // char c=5;
    // double d=5;
    // bool e=true;
}       